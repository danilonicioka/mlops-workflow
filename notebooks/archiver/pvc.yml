apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-pv-claim
  namespace: kubeflow-user-example-com
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi